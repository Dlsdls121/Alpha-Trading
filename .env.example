# fixture = simulated offline data (default, safe anywhere)
# live    = NSE option chains + Yahoo historical OHLCV
ALPHA_DATA_MODE=fixture

# 0.0.0.0 makes the dashboard reachable from other devices on your network
ALPHA_HOST=0.0.0.0
ALPHA_PORT=8000
